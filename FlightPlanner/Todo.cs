namespace FlightPlanner
{
    public class Todo
    {
        // TODO:
        // - create extension methods, for example:
        //      > string.Normalize() -> stirng.ToLower().Trim()
        //      > a bunch of verification stuff, get that out of FlightStorage!
        // - clean up imports
        // - go through controllers, seperate logic into smaller methods
        // - make basic auth less annoying
        // - make a constructor for all models
        // - remove weather forecast, what even is that
        // - clean up everything
    }
}
